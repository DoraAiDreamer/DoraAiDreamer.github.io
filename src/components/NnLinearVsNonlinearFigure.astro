---
// 线性模型只能画出直线，拟合不了弯曲/环状的非线性关系；神经网络叠线性+激活能拟合任意形状
---
<figure>
  <svg viewBox="0 0 600 240" width="100%" xmlns="http://www.w3.org/2000/svg">
    <g font-family="-apple-system,'PingFang SC','Microsoft YaHei',sans-serif">
      <text x="300" y="24" font-size="14" font-weight="bold" fill="#0c5f74" text-anchor="middle">为什么线性模型不够：直线画不出弯曲的决策边界</text>

      <!-- 左：线性（一条直线，无法分环状） -->
      <rect x="40" y="42" width="240" height="160" rx="12" fill="#fafcfd" stroke="#dce8ef"></rect>
      <text x="160" y="68" font-size="12" font-weight="bold" fill="#b14a4a" text-anchor="middle">线性模型：只能画一条直线</text>
      {/* 外环点 + 内圈点（环状分布，直线分不开） */}
      {[[120,140],[200,140],[160,100],[160,180],[130,110],[190,170],[130,170],[190,110]].map(([cx,cy]) => (
        <circle cx={cx} cy={cy} r="4.5" fill="#7ec3d8"></circle>
      ))}
      <circle cx="160" cy="140" r="4.5" fill="#f0a35e"></circle>
      <circle cx="175" cy="128" r="4.5" fill="#f0a35e"></circle>
      <circle cx="148" cy="152" r="4.5" fill="#f0a35e"></circle>
      {/* 一条硬切的直线 */}
      <line x1="100" y1="120" x2="220" y2="160" stroke="#d9504f" stroke-width="2.5" stroke-dasharray="6,4"></line>
      <text x="160" y="210" font-size="10" fill="#b14a4a" text-anchor="middle">一条虚线怎么切都会切错两类</text>

      <!-- 右：非线性（弯曲边界可以分） -->
      <rect x="320" y="42" width="240" height="160" rx="12" fill="#f7fbf9" stroke="#bfe6d3"></rect>
      <text x="440" y="68" font-size="12" font-weight="bold" fill="#0a7a50" text-anchor="middle">神经网络：线性 + 激活可拼出曲线</text>
      {[[400,140],[480,140],[440,100],[440,180],[410,110],[470,170],[410,170],[470,110]].map(([cx,cy]) => (
        <circle cx={cx} cy={cy} r="4.5" fill="#7ec3d8"></circle>
      ))}
      <circle cx="440" cy="140" r="4.5" fill="#f0a35e"></circle>
      <circle cx="455" cy="128" r="4.5" fill="#f0a35e"></circle>
      <circle cx="428" cy="152" r="4.5" fill="#f0a35e"></circle>
      {/* 椭圆弯曲边界 */}
      <ellipse cx="441" cy="140" rx="28" ry="24" fill="none" stroke="#0f9d68" stroke-width="2.5" stroke-dasharray="6,4"></ellipse>
      <text x="440" y="210" font-size="10" fill="#0a7a50" text-anchor="middle">非线性边界可以把两类分开</text>
    </g>
  </svg>
</figure>
